import React from 'react';
import {View, Text} from 'react-native';

const Header = header => {
  return (
    <View>
      <Text>este é nosso header</Text>
    </View>
  );
};

export default Header;
